{
  "name": "verktoykassen",
  "version": "0.1.0",
  "private": true,
  "description": "Prototype-løsning for en verktøykasse med sanity",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/navikt/verktoykassen.git"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/navikt/verktoykassen/issues"
  },
  "scripts": {
    "postinstall": "husky install",
    "dev": "yarn workspace website dev",
    "build:next": "yarn sb:build && yarn workspace website build",
    "serve:next": "yarn workspace website start",
    "deploy:vercel": "yarn workspace website deploy:vercel",
    "deploy": "yarn workspace website deploy",
    "sanity": "yarn workspace sanity start",
    "sanity:deploy": "yarn workspace sanity deploy",
    "sanity:update:nav": "yarn workspace sanity update:nav",
    "lint": "yarn workspace website lint",
    "sb": "yarn workspace storybook storybook",
    "sb:build": "yarn workspace storybook build-storybook",
    "sb:watch": "yarn workspace storybook storybook:watch"
  },
  "workspaces": [
    "sanity",
    "website",
    "storybook"
  ],
  "prettier": {},
  "dependencies": {
    "@navikt/ds-css": "^0.9",
    "@navikt/ds-icons": "^0.5",
    "@navikt/ds-react": "0.10",
    "@navikt/ds-tokens": "^0.5"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "husky": "^7.0.2",
    "pretty-quick": "^3.1.1"
  }
}
