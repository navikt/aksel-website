import { Button } from "@navikt/ds-react";
import { withDsExample } from "components/website-modules/examples/withDsExample";

const Primary = () => {
  return <Button>Primary</Button>;
};

export default withDsExample(Primary);
